"""
Step 5 entry point.

Run with:  python -m scripts.run_eval

Runs the ablation sweep (chunk size x embedding model x retrieval mode)
against eval/qa_pairs.json and prints/saves a comparison table of
retrieval precision/recall + faithfulness per configuration.
"""

if __name__ == "__main__":
    raise NotImplementedError("Step 5: wire up app.eval.metrics ablation runner here.")
